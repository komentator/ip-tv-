<Window x:Class="IpTvPlayer.Views.EpgSearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Поиск программ EPG" Height="600" Width="780"
        Background="#1e1e1e" Foreground="White"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Поиск программ передач" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" x:Name="QueryBox" Padding="8" Background="#3c3c41" Foreground="White" FontSize="13" KeyDown="QueryBox_KeyDown"/>
            <Button Grid.Column="1" Content="🔎 Найти" Margin="10,0,0,0" Padding="15,5" Background="#0e639c" Foreground="White" Click="Search_Click" IsDefault="True"/>
        </Grid>

        <ListBox Grid.Row="2" x:Name="ResultsList" Background="#252526" BorderThickness="0" Foreground="White"
                 ScrollViewer.VerticalScrollBarVisibility="Auto" MouseDoubleClick="ResultsList_MouseDoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Padding="10,8" BorderThickness="0,0,0,1" BorderBrush="#3e3e42">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding StartTime, StringFormat='dd.MM HH:mm'}" Foreground="#ffc107" FontWeight="Bold" Margin="0,0,10,0"/>
                                <TextBlock Text="{Binding ChannelTvgId}" Foreground="#4caf50" FontSize="11"/>
                            </StackPanel>
                            <TextBlock Text="{Binding Title}" FontSize="13" FontWeight="Bold" Margin="0,3,0,0" TextWrapping="Wrap"/>
                            <TextBlock Text="{Binding Description}" FontSize="11" Foreground="#cccccc" TextWrapping="Wrap" Margin="0,3,0,0"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" x:Name="StatusText" FontSize="11" Foreground="#888888" VerticalAlignment="Center"/>
            <Button Grid.Column="1" Content="Закрыть" Width="100" Padding="8,5" Background="#3e3e42" Foreground="White" Click="Close_Click" IsCancel="True"/>
        </Grid>
    </Grid>
</Window>
