<Window x:Class="IpTvPlayer.Views.EpgScheduleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Программа передач" Height="600" Width="700"
        Background="#1e1e1e" Foreground="White"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock x:Name="ChannelName" FontSize="18" FontWeight="Bold"/>
            <TextBlock x:Name="DateRange" FontSize="11" Foreground="#888888" Margin="0,4,0,0"/>
        </StackPanel>

        <ListBox Grid.Row="1" x:Name="ProgramsList" Background="#252526" BorderThickness="0"
                 ScrollViewer.VerticalScrollBarVisibility="Auto" Foreground="White">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Padding="10,8" BorderThickness="0,0,0,1" BorderBrush="#3e3e42">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding TimeRange}" FontWeight="Bold" Foreground="#ffc107"/>
                                <TextBlock Text="{Binding Category}" FontSize="10" Foreground="#888888"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding Title}" FontSize="13" FontWeight="Bold" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding Description}" FontSize="11" Foreground="#cccccc" TextWrapping="Wrap" Margin="0,3,0,0"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Background" Value="Transparent"/>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Закрыть" Width="90" Padding="8,5" Background="#0e639c" Foreground="White" Click="Close_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
