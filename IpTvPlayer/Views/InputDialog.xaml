<Window x:Class="IpTvPlayer.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ввод" Height="170" Width="440"
        Background="#252526" Foreground="White"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" x:Name="PromptText" FontSize="13" Margin="0,0,0,8"/>
        <TextBox Grid.Row="1" x:Name="InputBox" Padding="8" Background="#3c3c41" Foreground="White" FontSize="13"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" Width="90" Margin="0,0,10,0" Padding="8,5" Background="#3e3e42" Foreground="White" Click="Cancel_Click" IsCancel="True"/>
            <Button Content="ОК" Width="100" Padding="8,5" Background="#0e639c" Foreground="White" Click="Ok_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
